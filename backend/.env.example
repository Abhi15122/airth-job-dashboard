# Dedicated AIRTH database. Keep Neon's TLS parameters in the URL.
DATABASE_URL=
PORT=3000
FRONTEND_ORIGIN=http://localhost:5173
# Separate disposable database ONLY; integration tests refuse DATABASE_URL.
TEST_DATABASE_URL=
